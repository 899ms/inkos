# 从创作结果重新审视 Harness

2026-09-10。授权目标：创作结果和用户交互流畅度优先，允许替换不合适设计。本轮不以现有模块数量、测试数量或某个模型一次成功作为设计正确的依据。

## 判断

部分问题确实由 Harness 设计带来，而且不只是提示词不够严格。它把通用文件工具、领域生产工具和前端显示数据拼在一起，但给主模型的接口没有形成一致的作品、版本和结果语义。模型因此承担了路径转换、结果解释和继续执行状态的推断。重复添加“不要猜”“不要误报”只能降低偶发率，不能消除这类任务。

仍然需要分开判断三类问题：通用DeepSeek名称的实际路由失败有网关/上游证据；稿件因果和商业回报问题需要生产方法与审稿证据；跨作品、越界修订、版本选择和状态恢复是系统责任。不能把模型API兼容性或市场质量全归因于Pi主循环。

## 证据与结构性处理

| 观察 | 设计责任 | 处理 |
| --- | --- | --- |
| 当前上下文给产物ID，read只接收路径；v42将scene产物ID拼成不存在的文件 | 作品接口没有贯通，主模型被迫理解存储结构 | 已改：read直接接收产物ID，默认绑定当前Work；由统一读取器解析注册版本 |
| source路径可能指向最新候选，用户要读的却是已采纳版本 | 文件位置和版本身份混用 | 已改：读取与导出共用校验后的版本快照，分页携带revisionId；候选必须显式指定 |
| 领域工具保存delivery、数量和选项，但模型只看到content和拼接的ID | 主模型观察是有损适配；UI知道的事实主模型不知道 | 已替换旧文本拼接器：统一结构化观察，操作状态、交付检查、版本和选项分别保留 |
| 原回执有code/recovery，异常在工具边界只剩一句话 | 恢复协议没有传到底 | 已改：有类型的错误在模型边界保留code和recovery，底层Episode继续保留原始错误 |
| 压缩进度和即时工具观察各自挑选事实 | 两份选择逻辑会漂移 | 已合并事实投影；不复制完整正文、图谱或历史到进度记录 |
| 互动选项恢复依赖会话回执，改正文曾重生成选项 | 领域当前呈现没有完整、独立的持久化边界 | 已改：场景与选项保存为同一presentation记录，检查点和版本携带该记录；旧回执读取隔离到兼容模块 |
| 影游整图包、长文嵌套JSON需要反复纠错 | 大规模内容、拓扑结构、提交协议耦合 | 保持拓扑由系统校验、正文按领域边界提交；根据失败分布决定替换整包生成路径 |
| 新建Work、会话绑定和生产继续由多处状态推动 | 前端会话与作品执行目标重复建模 | 已统一：创建后先持久化执行目标，agent、成功/失败响应和重试使用该目标；页面根据会话状态完成导航 |

## 本轮重构边界

交付一个跨类型纵向闭环：当前Work上下文给出可直接调用的读取目标 → Harness read → 已采纳版本/显式候选 → 校验与分页 → 结构化观察 → 压缩进度。脚本、分镜、翻译、短篇及其他登记文本均用同一边界。导出复用同一个版本读取器。

没有再增加一个与read重叠的“智能读取”工具。现有read接口获得产物寻址；路径形式保留给上传素材、项目文件和旧调用。旧的纯文本回执拼接逻辑已被替换，不与新协议并行。现有UI详情及Episode原始回执继续保留，避免把模型观察变成第二份领域数据库。

这不是全部Harness重构完成。实际创作质量、模型的首次完成率和浏览器交互仍需真实重跑；当前测试只证明模块合同和状态边界。特别是“Episode completed”表示本轮循环结束，不代表作品交付通过，验收不能再从这个字段推断成功。

## 历史回执复放

v40/v41/v42共284条真实成功动作的回执用于离线协议复放，未调用模型。15个交付状态、12个请求/完成数量、48组原始选项均完整保留其控制语义（75/75）。原始领域数据与完整证据仍保存在Episode及产物中。

观察总量由628275字节变为788911字节，增加约25.6%；这项改动没有取得上下文成本下降的证据。最初直接复制完整结构化信息时曾达到1118030字节；最终模型观察去除了重复目录路径、长篇引用和审稿方法描述，保留问题摘要、来源ID、目标版本和交付状态。必须让真实模型旅程来判断这些新增信息是否抵得过其成本，而不能把JSON化本身当成成功。

复放脚本与逐动作统计在 `output/inkos-harness-redesign-2026-09-10/replay.mjs` 和 `replay.json`。它们只证明接口传递的信息，没有测量pass@1、误改率或用户体验。

## 验证方式调整

1. 每个结构性变更先走短小的真实模块闭环：正确Work、正确版本、范围保护、失败后恢复。避免按13类复制同一个实现断言。
2. 用历史真实回执复放检查观察信息损失和体积，不调用模型，不冒充真实用户成功率。
3. 获得数据外发许可后，先跑有代表性的真实旅程：线性创作与局部修改、跨Work派生、互动状态与正文修改。记录用户补充次数、模型失败次数、重复读取、误改和端到端耗时。
4. 这些代表旅程稳定后才冻结候选做一次完整13类发布验收；不为每个局部修复从头跑整张矩阵。
5. 内容硬伤遵守系统审稿与人读双确认；审美偏好和单次失败不得直接变成全局提示词规则。商业质量与真实市场效果分别判断。

## 下一轮的决策门槛

先看上述纵向改动是否减少模型往返和错误，再决定互动呈现快照、会话绑定、生产宏动作的替换范围。若同一责任在两个模块重复承担，优先合并责任或删除一条路径；若失败仅是服务路由或模型输出限制，修适配层，不扩张核心编排。每次只保留能证明结果改善的改动。

## 第二批验证

互动呈现快照与跨会话读取已落地。前端从聊天消息反查选项的逻辑及其六项旧测试删除，领域选项和API闭环接替；当前1029项测试通过。真实浏览器打开独立本地测试作品、新建会话、刷新页面后，仍显示同一场景与两个选项。新会话ID与Work ID不同，读取目标保持Work ID。没有调用模型，因此只能据此确认本地交互和恢复合同。

证据见 `output/inkos-harness-redesign-2026-09-10/presentation/`。通用创建后生产继续、真实模型成功率和商业内容质量尚不属于本轮新增结论。

## 第三批验证

通用创建后的目标转换、异常恢复和重试已完成重构。删除Studio对工具结果和异常参数的重新绑定逻辑，改为读取核心提前提交的执行目标。自然语言与确认入口共用转换函数，旧目标比较并更新检查防止迟到请求覆盖新目标。

真实浏览器通过本机受控接口验证创建→继续失败→自动导航→重试导出→刷新，以及旧目标409保护。原作品内容保持，导出只产生1份。另将模型观察与用户可见文字分开，并标记内部继续消息的可见性。当前1033项测试及构建通过，证据在 `output/inkos-harness-redesign-2026-09-10/execution-target/`。

本轮未调用外部模型。接下来的价值判断应来自真实模型代表旅程与内容结果，避免继续围绕受控测试扩大重构范围。

## 第四批：真实旅程后的共享契约修复（2026-09-12）

真实基线完成两个模型的剧本、分镜及互动代表旅程。四份文本最终有对应版本审稿和一致导出；局部修改保留了剧本前两场和分镜前五镜，派生阶段原剧本文件哈希不变。首次任务仍有遗漏、绕路和超长稿误判，不能据此声称首次完成或内容验收通过。GPT互动五次行动、正文改写、新会话、刷新和历史恢复均验证；Opus完成前述动作及改写恢复显示，但历史版本恢复请求连续被上游分组403拒绝。原始失败和最终版本分别保存在 `output/inkos-harness-real-2026-09-12/`。

本批修复以同一责任共享实现为边界：

- 读取、编辑和审稿引用共用物理源文件分行函数，消除末尾换行造成的253/252行差异；产物读取直接给出编号，编辑错误提供包含Work、产物和版本的恢复地址。
- 审稿和修改通过统一注册版本读取器取快照并验证校验和；测量完整原文的汉字、非空白字符、英文词及行数，把同一份宿主测量交给读者模型、审稿模型、持久化报告及压缩后的执行进度。测量范围写明为完整产物，不能冒充正文某一段的精确计数。
- 首次剧本/分镜/影游生产按已有产物判断是否可执行，不再用Work的active/draft生命周期代替“是否已有成稿”。只有简报的active Work可继续首次生产；已有当前生产稿、归档作品和未登记生产文件保持保护，并返回稳定错误及恢复指向。

本地验证共1034项通过（Core651、Studio295、CLI88），全包构建通过。新增一个“通用创建→专业生产→阻止覆盖”闭环；现有编辑闭环改用读取返回的末行地址，并在候选审稿闭环核对请求、报告和回执的测量一致。

修复候选 `contracts-2026-09-12`，源码摘要 `682d640f73d7ac058fffcb169a64880eba78b58e8206a7b3534e19da6d8a493f`，734个源码文件、2257个构建文件。用户随后明确授权具体测试稿件与运行上下文经 kkaiapi 复验，已完成真实请求。完整原稿测量分别为1592和2670汉字，父稿保持；派生流程仍出现多余确认、重复提示词和提前结束，原始失败已保留。

仍待解决的系统问题：复合创作请求在局部动作成功后提前结束；审稿失败有时导致重新创作；流式生产期间界面等待结束才切换作品目标。需要以真实改进证据决定下一步闭环设计，不能从局部接口修复推断13类同候选发布就绪。

## 第五批：动作身份与审稿来源（2026-09-13）

真实故障推动了三处责任收敛：确认动作的可见范围与执行策略共用判断；专业生产固定当前 Work，创建身份由通用创建动作负责；跨作品派生通过产物版本引用保存原文快照。审稿自动读取该类型生产包的原稿、规格与配套文档，并明确缺失证据，避免把独立文档误判为正文缺项。

`work-boundary-2026-09-13` 中 Opus 已实跑完整派生和局部修改。网关追踪同时确认渠道 29 的分组403与渠道28的具名工具选择400；同上下文通过渠道42成功。InkOS 的单工具结构化提交改为 required，仍由结果 schema 校验。未改线上渠道。

`review-context-2026-09-13`（摘要 `cbc53a7661e858ba55b68fbc53723ee58c68982042e0bc780dc4e9f6b3cff0ec`）中两模型均正确绑定作品、固定原稿版本、加载完整审稿上下文。1037项测试及构建通过。Opus首次派生完整交付；GPT首次遗漏导出，第二次读取后返回空格；Opus修改后遗漏专业审稿。不能把这些失败算作代表旅程全部通过。

最后两项是本轮新增设计责任：空白文本也必须遵循传输重试预算；“审稿并导出”应有同一版本的完整执行回执。详细记录分别在 `output/inkos-harness-work-boundary-2026-09-13/report.md` 和 `output/inkos-harness-review-context-2026-09-13/report.md`。

## 第六批：同一版本的交付动作

对通用文本作品增加同一版本的审稿导出动作：先固定版本，持久化专业审稿，再导出该版本；审稿失败或版本冲突时停止，已经保存的正文和审稿不丢失。质量发现与动作执行分别返回，导出有问题的候选不等于内容通过。

纯空白文本和只有思考内容的结束响应纳入同一传输重试预算，耗尽后返回 `MODEL_EMPTY_RESPONSE`。既有测试闭环增加了空数组后空白文本、审稿失败后保持旧导出、审稿版本过期拒绝导出的验证。

本批冻结候选、完整测试结果和两模型同指令真实复验记录集中在 `output/inkos-harness-delivery-2026-09-13/`；是否通过以其中 `report.md` 和版本证据为准，不从工程改动推断首次完成率。

该轮发现可选交付模式仍被误选，且修改成功回执缺少实际改动。当前接口改为名称明确的 `review_and_export_work_artifact`，必定完成两步；`review_work_artifact` 和 `export_work` 各自只执行单步。修改回执包含新版本及有长度上限的真实变化区域，供主模型核对请求是否落实。新候选从已保存的稿件检查点复验同一末镜修改指令，记录在 `output/inkos-harness-explicit-delivery-2026-09-13/`。

该恢复复验中两模型均一次请求完成当前版本审稿和导出，父稿与前五镜保持。GPT自行纠正一次导出副本目标错误；随后系统将导出副本设为不可直接修订，在进入写作模型前返回正文来源。最后这项边界验证及冻结候选记录在 `output/inkos-harness-delivery-boundary-2026-09-13/`。产品审稿仍缺少历史版本对照证据，当前外部哈希核验不能冒充产品内部已具备该能力；完整13类和内容市场验收仍有缺口。

## 第七批：审稿直接核验版本差异

`delivery-boundary-2026-09-13`在用户明确允许后补齐GPT：156.9秒、8次模型调用、0次动作错误，当前版本审稿与导出完成，外部核对只有第75行机位运动变化。但审稿仍报缺少旧版，说明历史版本已经保存却未进入专业审稿输入。

当前执行上下文保留每轮开始时的作品快照，并与可更新的当前作品分开。审稿自动读取该起始版本的校验后快照、当前原文及实际差异；同轮多次修改不会把比较起点推进到中间稿。没有执行上下文的单独审稿只比较登记的父版本，并明确标识该范围。比较范围、版本、哈希和差异随审稿报告、动作回执及压缩后的进度保存。新请求会建立新的基线。

该变更沿用原有Work/Artifact/Revision与执行上下文，没有添加要求用户填写的对照参数。模块闭环覆盖首次生产、导出副本拒绝、审稿失败、同轮两次修改后的起点保持及下一轮基线刷新。冻结候选和真实复验结果在 `output/inkos-harness-version-review-2026-09-13/`。

该候选两模型均实跑成功使用起始版本对照，当前稿审稿与导出一致，前五镜及父稿保持。GPT仍因审稿时未拿到未来的导出回执而返回unavailable，宿主却把所有审稿观察归入quality。当前修复沿用既有的quality/execution分类：审稿显式分类，完整原报告保留；组合交付根据内容发现计算质量状态，以实际工具回执确定审稿与导出执行，并返回确定性的版本、路径和状态。模型在审稿阶段对执行状态的判断单独记录，不覆盖随后产生的执行事实。最终验证记录在 `output/inkos-harness-delivery-facts-2026-09-13/`。

最终候选 `delivery-facts-2026-09-13`（摘要 `f9e0039e0090bf347b6da4bfb9df4f354d49e4338edd9267fbd153ee13324fad`）中，两模型均一次请求完成局部修改、产品内部版本对照、审稿和同版本导出，动作错误为0；父稿、前五镜和标注60秒时长保持。GPT审稿中的执行观察完整保留，已完成的导出不再被误报成内容未验证。1037项本地测试及构建通过，CLI冷启动超时后的低并行重跑记录保留。该结论限于本轮局部交付，完整13类与内容市场验收仍需完成。
